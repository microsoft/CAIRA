{
  "name": "caira-api-typescript",
  "private": true,
  "type": "module",
  "version": "0.1.0",
  "description": "CAIRA Business API container — business-oriented proxy to agent container",
  "scripts": {
    "start": "node src/server.ts",
    "dev": "node --watch src/server.ts",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "lint": "eslint .",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@microsoft/opentelemetry": "^1.0.2",
    "@azure/identity": "^4",
    "@opentelemetry/api": "^1.9.0",
    "fastify": "^5",
    "jose": "^6.1.0"
  },
  "overrides": {
    "@opentelemetry/sdk-node": "0.218.0"
  },
  "devDependencies": {
    "@types/node": "^22",
    "typescript": "~5.8",
    "vitest": "^3"
  },
  "engines": {
    "node": ">=24.0.0"
  }
}
